# Frontend (React + Vite) skeleton for OASIS

This frontend is a thin React + TypeScript application designed to consume OASIS APIs.

Key features implemented:
- Authentication integration with auth-service
- Paper search UI connected to paper-service
- Lightweight configuration for production builds (Vercel/Docker)

Run locally:
- cd frontend
- npm install
- npm run dev
